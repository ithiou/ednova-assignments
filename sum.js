const x1 = 10;
const x2 = 20;

const sum = x1 + x2;

const x = 25;

if (sum>x) {
    console.log(`La somme (${sum}) est supérieur à ${x}.`);
} else {
    console.log(`La somme (${sum}) n est pas supérieur à ${x}.`);
}